{
  "type": "object",
  "properties": {
    "opencartUsername": {
      "type": "string",
      "title": "Username",
      "value": "wp_user",
      "form": true,
      "validation": "^[a-z0-9_]*$",
      "description": "Allowed only lowercase characters, numbers and underscore"
    },
    "opencartPassword": {
      "type": "string",
      "title": "Password",
      "value": "password",
      "hidden": true,
      "form": true,
      "validation": "^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[^a-zA-Z0-9])(?!.*\s).{10,}$",
      "description": "Must contain at least 1 number, 1 special character, 1 uppercase and lowercase letter with 10 or more length"
    },
    "opencartEmail": {
      "type": "string",
      "title": "Admin email",
      "value": "admin@admin.com",
      "validation":"^[^\s@]+@[^\s@]+\.[^\s@]+$",
      "form": true
    },
    "clients_fqdn": {
      "type": "string",
      "title": "Custom Domain",
      "value": "",
      "form": true,
      "validation":"([a-z0-9|-]+\.)*[a-z0-9|-]+\.[a-z]+",
      "description": "You can provide your custom subdomain name here"
    },
    "mariadb": {
      "type": "object",
      "title": "MariaDB Details",
      "form": true,
      "properties": {
        "db": {
          "type": "object",
          "properties": {
            "user": {
              "type": "string",
              "value": "mariadb_user",
              "form": true,
              "title": "Database Username",
              "validation": "^[a-z0-9_]*$",
              "description": "Allowed only lowercase characters, numbers and underscore"
            },
            "password": {
              "type": "string",
              "form": true,
              "value": "mariadb_password",
              "hidden": true,
              "title": "Database Password",
              "validation": "^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[^a-zA-Z0-9])(?!.*\s).{10,}$",
              "description": "Must contain at least 1 number, 1 special character, 1 uppercase and lowercase letter with 10 or more length"
            },
            "database": {
              "type": "string",
              "form": true,
              "title": "Database Name",
              "value": "wp_database",
              "validation": "^[a-z0-9_]*$",
              "description": "Allowed only lowercase characters, numbers and underscore"
            }
          }
        }
      }
    }
  }
}
